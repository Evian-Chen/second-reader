import { useAuthStore } from './auth'
import { useBaseStore } from './base'
import { useBookStore } from './book'

export const useAuth = useAuthStore
export const useBase = useBaseStore
export const useBook = useBookStore
